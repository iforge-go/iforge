# 项目级 npm 配置
# React 19 + Chakra UI 2 / framer-motion 10 存在 peer dependency 冲突，
# 用 legacy-peer-deps 让 npm 沿用 npm v6 行为，按 lock 文件安装而不强制解析 peer。
# 本地 npm install 和 Docker 内 npm ci 均自动生效。
legacy-peer-deps=true
